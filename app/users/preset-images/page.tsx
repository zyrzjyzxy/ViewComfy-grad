'use client';

import { PresetImageManager } from '@/components/preset-image-manager';

export default function PresetImagesPage() {
    return (
        <div className="h-full overflow-auto">
            <PresetImageManager />
        </div>
    );
}
